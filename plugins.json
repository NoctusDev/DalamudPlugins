{
  "_comment": "The source list. Each entry names a plugin repo that owns a single-entry repo.json at the path given. pluginmaster.json is generated by concatenating them, so a plugin's own repo stays the source of truth for its description, tags and download links. Add a plugin here and nothing else changes.",
  "plugins": [
    { "owner": "NoctusDev", "repo": "buddys-buddy",   "branch": "main", "path": "repo.json" },
    { "owner": "NoctusDev", "repo": "MarketTraveler", "branch": "main", "path": "repo.json" }
  ]
}
